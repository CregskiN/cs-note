#include <iostream>

/*
 *  第二章小结
 * + 地址 addredd
 * + 别名声明 alias declaration
 *      typedef、using
 * + 算数类型 arithmetic type
 *      bool、char、int、float、double等内置类型
 * + 数组
 * + auto
 *      是类型说明符
 * + 基本类型 base type
 *      是类型说明符，可用const修饰
 *      在声明语句中，位于声明符之前
 *      基本数据类型提供了最常见的数据类型，以此为基础构建声明符
 * + 绑定 bind
 * + 字节 byte
 * + 类成员 class member
 * + 复合类型 compound type
 *      以其它类型为基础
 * + const
 *      是一种类型修饰符
 * + 常量指针 const pointer
 * + 常量引用 const reference
 *      指向常量的引用 rederence to const
 * + 常量表达式 const expression
 *      在编译期计算并获取结果的表达式
 * + constexpr 
 *      一种函数，用于代表一条常量表达式
 * + 转换 conversion
 * + 数据成员 data member
 * + 声明 declaration
 *      声称存在一个变量、函数，或是别出定义的类型
 *      名字必须在定义或声明之后
 * + 声明符 declarator
 *      是声明的一部分
 *      包括被定义的名字和类型修饰符。其中，类型修饰符可有可无
 * + decltype
 *      是类型说明符
 *      从变量或表达式推断得到类型
 * + 默认初始化 default initialization
 *      当对象未被显式地赋予初始值时执行的初始化行为，分以下三种：
 *          - 类本身负责执行的类对象的初始化行为
 *          - 全局作用于的内置类型对象初始化为0
 *          - 局部作用于的对象未被初始化，即拥有未定义的值
 * + 定义 definition
 *      未某以特定类型的变量申请存储空间，可选择同时初始化该变量
 *      变量名必须在定义或声明后才能用
 * + 转义序列 escape sequence
 * + 全局作用域
 * + 头文件保护符 header guard
 *      #ifden #else #endif
 * + 标志符 identifier
 * + 类内初始值 in-class initializer
 * + 列表初始化 list initialization 
 *      利用花括号，把一个或多个初始值放在一起的初始化形式
 * 
 * + 类型说明符 decltype、auto
 * + 类型修饰符 const、*、&、signed｜unsigned、long｜short
 * + 基本数据类型
 *      - 算数类型 arithmetic type：bool、char...
 *      - 空类型 void
 * + 一条声明语句 = 一个基本数据类型base type 和 一个声明符列表declarator 组成
 *      
 *      
 */

int main() {
    
    return 0;
}
